// Package processor provides built-in request processors for instruction resolution,
// content assembly, and other pre-processing tasks in AgentMesh flows.
//
// These processors can be composed into flows to enhance agent capabilities
// without modifying core logic. Custom processors can also be implemented
// by adhering to the RequestProcessor and ResponseProcessor interfaces.
package processor
