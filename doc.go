// Package agentmesh provides a convenience façade over the lower-level packages
// (core, agent, runner, tool, etc.).  It exposes the most common entry points
// so callers can bootstrap, run, and extend AgentMesh applications without
// digging through subpackages.
package agentmesh
