// Package callbacks provides plugin management and context helpers for intercepting
// and modifying agent execution at various lifecycle points (model calls, tool executions,
// node execution, and state changes).
package callbacks
